#include <iostream>
using namespace std;

int main (){
    float length, width;
    cout << "Enter the Length of the Rectangle :" << endl;
    cin >> length;
    cout << "Enter the width of the Rectangle :" << endl;
    cin >> width;
    cout << "The Area of the given Rectangle is " << length*width << endl;
    return 0;
}